-- CANDIDATO FUTURO DOCUMENTADO - NO EJECUTAR TODAVIA.
-- NO APLICADO.
-- NO CREA FIXTURES REALES.
-- NO VERIFICA RLS CON USUARIOS AUTH.
-- NO PRODUCCION.
-- NO CONTIENE DATOS REALES.
-- NO CREA USUARIOS AUTH.
-- NO INCLUYE RESET.
-- NO USAR CONTRA PRODUCCION.
--
-- Este archivo es un plan de fixtures sinteticos para revision futura.
-- Requiere un entorno Supabase aislado/desechable y aprobacion posterior.
-- Todas las plantillas de operaciones estan comentadas a proposito.
-- No debe existir ningun INSERT/UPDATE/DELETE ejecutable por defecto.
--
-- Seguridad:
-- - No usar datos privados reales.
-- - No usar emails reales.
-- - No usar tokens, keys ni service-role.
-- - No usar project ref real.
-- - No usar local-yori, local-ale ni distancia-cero-local-space como UUIDs remotos.
-- - No crear filas internas de autenticacion desde este fixture.
-- - No mezclar este plan con SQL de reset.
-- - No ejecutar contra un entorno no desechable.
--
-- Estado S4.6.4.1:
-- - Schema y RLS fueron aplicados manualmente en un laboratorio desechable
--   segun evidencia humana documentada.
-- - Este fixture no fue aplicado.
-- - El reset no fue aplicado.
-- - No hay Auth users sinteticos creados por este archivo.
-- - Storage y la app siguen fuera de alcance.
-- - SQL Editor corre con privilegios y no representa un cliente autenticado
--   normal.
-- - Cualquier aplicacion futura requiere otro GO explicito.

-- ============================================================================
-- 1. Prerrequisitos documentales
-- ============================================================================
--
-- Antes de convertir este plan en SQL ejecutable, una fase posterior debe
-- confirmar explicitamente:
--
-- - docs/supabase/schema_draft.sql fue revisado y aplicado manualmente en un
--   laboratorio Supabase aislado/desechable.
-- - docs/supabase/rls_draft.sql fue revisado y aplicado manualmente en ese
--   mismo tipo de laboratorio.
-- - El comportamiento de RLS con usuarios sinteticos y memberships todavia
--   debe verificarse en una fase futura.
-- - Las identidades Auth sinteticas fueron creadas o mapeadas por un flujo
--   aprobado, sin datos reales.
-- - Los UUIDs de abajo fueron reemplazados por UUIDs reales del entorno
--   desechable.
-- - El rollback principal sigue siendo destruir el proyecto Supabase
--   desechable.
--
-- Estos placeholders NO son valores reales:
--
-- <owner_a_auth_uuid>
-- <partner_a_auth_uuid>
-- <owner_b_auth_uuid>
-- <external_user_auth_uuid>
-- <space_a_uuid>
-- <space_b_uuid>
-- <space_empty_uuid>
-- <item_reason_a_uuid>
-- <item_promise_a_uuid>
-- <item_important_date_a_uuid>
-- <item_future_dream_a_uuid>
-- <item_timeline_a_uuid>
-- <item_gallery_a_uuid>
-- <item_playlist_a_uuid>
-- <item_monthly_letter_a_uuid>
-- <item_open_when_a_uuid>
-- <item_hidden_a_uuid>
-- <item_override_a_uuid>
-- <media_a_uuid>
-- <event_a_uuid>

-- ============================================================================
-- 2. Profiles sinteticos
-- ============================================================================
--
-- Plantilla futura, NO EJECUTABLE:
--
-- insert into public.profiles (id, local_slug, display_name, avatar_url)
-- values
--   ('<owner_a_auth_uuid>'::uuid, 'owner_a', 'Synthetic Owner A', null),
--   ('<partner_a_auth_uuid>'::uuid, 'partner_a', 'Synthetic Partner A', null),
--   ('<owner_b_auth_uuid>'::uuid, 'owner_b', 'Synthetic Owner B', null),
--   ('<external_user_auth_uuid>'::uuid, 'external_user', 'Synthetic External User', null);
--
-- Nota:
-- - admin_actor_conceptual es solo una nota de flujo administrativo futuro.
-- - No debe usarse como usuario normal de frontend.
-- - No usar emails reales.
-- - No crear identidades Auth desde este archivo.

-- ============================================================================
-- 3. Relationship spaces sinteticos
-- ============================================================================
--
-- Plantilla futura, NO EJECUTABLE:
--
-- insert into public.relationship_spaces (id, name, slug, created_by)
-- values
--   ('<space_a_uuid>'::uuid, 'Synthetic Space A', 'space-a-synthetic', '<owner_a_auth_uuid>'::uuid),
--   ('<space_b_uuid>'::uuid, 'Synthetic Space B', 'space-b-synthetic', '<owner_b_auth_uuid>'::uuid);
--
-- Nota:
-- - space_empty queda como caso opcional, sin datos asociados.
-- - Si se usa en una fase futura, debe permanecer sintetico y no privado.
--
-- Plantilla opcional futura, NO EJECUTABLE:
--
-- insert into public.relationship_spaces (id, name, slug, created_by)
-- values ('<space_empty_uuid>'::uuid, 'Synthetic Empty Space', 'space-empty-synthetic', '<owner_a_auth_uuid>'::uuid);
--
-- Nota:
-- - No usar distancia-cero-local-space como UUID remoto.

-- ============================================================================
-- 4. Memberships sinteticos
-- ============================================================================
--
-- Plantilla futura, NO EJECUTABLE:
--
-- insert into public.universe_members (space_id, user_id, role)
-- values
--   ('<space_a_uuid>'::uuid, '<owner_a_auth_uuid>'::uuid, 'owner'),
--   ('<space_a_uuid>'::uuid, '<partner_a_auth_uuid>'::uuid, 'partner'),
--   ('<space_b_uuid>'::uuid, '<owner_b_auth_uuid>'::uuid, 'owner');
--
-- Nota:
-- - external_user queda autenticado conceptualmente pero sin membership.
-- - El bootstrap owner/partner real debe ser controlado por RPC/admin futuro.
-- - No permitir self-owner arbitrario desde cliente normal.

-- ============================================================================
-- 5. Content items validos
-- ============================================================================
--
-- 5.1 Reason local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_reason_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'reasons',
--   'synthetic-reason-a1',
--   null,
--   'local',
--   '{"text":"Synthetic reason A1"}'::jsonb,
--   1,
--   '<owner_a_auth_uuid>'::uuid,
--   '<owner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.2 Promise local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_promise_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'promises',
--   'synthetic-promise-a1',
--   null,
--   'local',
--   '{"text":"Synthetic promise A1"}'::jsonb,
--   1,
--   '<partner_a_auth_uuid>'::uuid,
--   '<partner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.3 Important date local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_important_date_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'importantDates',
--   'synthetic-date-a1',
--   null,
--   'local',
--   '{"date":"2099-01-01","title":"Synthetic date A1","description":"Synthetic date note A1"}'::jsonb,
--   1,
--   '<owner_a_auth_uuid>'::uuid,
--   '<owner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.4 Future dream local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_future_dream_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'futureDreams',
--   'synthetic-dream-a1',
--   null,
--   'local',
--   '{"title":"Synthetic dream A1","description":"Synthetic dream note A1"}'::jsonb,
--   1,
--   '<owner_a_auth_uuid>'::uuid,
--   '<owner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.5 Timeline local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_timeline_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'timeline',
--   'synthetic-timeline-a1',
--   null,
--   'local',
--   '{"date":"2099-02-01","title":"Synthetic timeline A1","description":"Synthetic timeline note A1"}'::jsonb,
--   1,
--   '<partner_a_auth_uuid>'::uuid,
--   '<partner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.6 Black hole gallery local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_gallery_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'blackHoleGallery',
--   'synthetic-gallery-a1',
--   null,
--   'local',
--   '{"date":"2099-03-01","title":"Synthetic gallery item A1","description":"Synthetic gallery note A1","image":"","alt":"Synthetic gallery item A1","tag":"synthetic"}'::jsonb,
--   1,
--   '<owner_a_auth_uuid>'::uuid,
--   '<owner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.7 Playlist local A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_playlist_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'playlist',
--   'synthetic-playlist-a1',
--   null,
--   'local',
--   '{"title":"Synthetic playlist item A1","artist":"Synthetic Artist","description":"Synthetic playlist note A1","sourceType":"external","src":"","link":"","tag":"synthetic"}'::jsonb,
--   1,
--   '<partner_a_auth_uuid>'::uuid,
--   '<partner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.8 Monthly letter local A1 - legacy/adaptador futuro
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_monthly_letter_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'monthlyLetters',
--   'synthetic-monthly-letter-a1',
--   null,
--   'local',
--   '{"month":"Synthetic month A1","title":"Synthetic monthly letter A1","content":"Synthetic monthly letter body A1"}'::jsonb,
--   1,
--   null,
--   null,
--   'synthetic-fixture-legacy-adapter'
-- );
--
-- Nota:
-- - monthlyLetters legacy no debe recibir autoria inventada.
-- - created_by/updated_by quedan null hasta que exista adaptador aprobado.
--
-- 5.9 Open when local A1 - legacy/adaptador futuro
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_open_when_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'openWhenLetters',
--   'synthetic-open-when-a1',
--   null,
--   'local',
--   '{"title":"Synthetic open when A1","mood":"synthetic","content":"Synthetic open when body A1"}'::jsonb,
--   1,
--   null,
--   null,
--   'synthetic-fixture-legacy-adapter'
-- );
--
-- Nota:
-- - openWhenLetters legacy no debe recibir autoria inventada.
-- - created_by/updated_by quedan null hasta que exista adaptador aprobado.
--
-- 5.10 Override valido A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_override_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'reasons',
--   null,
--   'base-reason-synthetic-a1',
--   'override',
--   '{"text":"Synthetic override reason A1"}'::jsonb,
--   1,
--   '<owner_a_auth_uuid>'::uuid,
--   '<owner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- 5.11 Hidden valido A1
--
-- insert into public.content_items (
--   id, space_id, collection, local_id, base_id, kind, data,
--   schema_version, created_by, updated_by, source
-- )
-- values (
--   '<item_hidden_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   'promises',
--   null,
--   'base-promise-synthetic-a1',
--   'hidden',
--   '{"reason":"Synthetic hidden marker A1"}'::jsonb,
--   1,
--   '<partner_a_auth_uuid>'::uuid,
--   '<partner_a_auth_uuid>'::uuid,
--   'synthetic-fixture'
-- );
--
-- Nota:
-- - hidden representa marker/restauracion futura, no hard delete por defecto.

-- ============================================================================
-- 6. Media assets sinteticos
-- ============================================================================
--
-- Plantilla futura, NO EJECUTABLE:
--
-- insert into public.media_assets (
--   id, space_id, content_item_id, bucket, path, mime_type, size_bytes, created_by
-- )
-- values (
--   '<media_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   '<item_gallery_a_uuid>'::uuid,
--   'relationship-media-synthetic',
--   'space-a/synthetic-gallery-a1.txt',
--   'text/plain',
--   128,
--   '<owner_a_auth_uuid>'::uuid
-- );
--
-- Nota:
-- - Esta fila solo representa metadata.
-- - No crea archivo real.
-- - storage.objects queda fuera de alcance.
-- - content_item_id debe pertenecer al mismo space_id.

-- ============================================================================
-- 7. Content events sinteticos
-- ============================================================================
--
-- Plantilla futura, NO EJECUTABLE:
--
-- insert into public.content_events (
--   id, space_id, content_item_id, collection, action, actor_id, payload
-- )
-- values (
--   '<event_a_uuid>'::uuid,
--   '<space_a_uuid>'::uuid,
--   '<item_reason_a_uuid>'::uuid,
--   'reasons',
--   'content_created',
--   '<owner_a_auth_uuid>'::uuid,
--   '{"fixture":"synthetic","sensitive":false}'::jsonb
-- );
--
-- Nota:
-- - Evento append-only conceptual.
-- - Payload minimo/sanitizado.
-- - Audit confiable requiere trigger/RPC/admin futuro.
-- - No tratar inserts libres del cliente como audit confiable.

-- ============================================================================
-- 8. Casos validos esperados - SOLO COMENTARIOS EXPECT PASS
-- ============================================================================
--
-- EXPECT PASS: owner_a puede leer content_items de space_a.
-- EXPECT PASS: partner_a puede leer content_items de space_a.
-- EXPECT PASS: owner_b puede leer content_items de space_b.
-- EXPECT PASS: content_items kind local con local_id y sin base_id.
-- EXPECT PASS: content_items kind override con base_id y sin local_id.
-- EXPECT PASS: content_items kind hidden con base_id y sin local_id.
-- EXPECT PASS: monthlyLetters/openWhenLetters legacy quedan sin autoria
--              inventada hasta que exista adaptador aprobado.
-- EXPECT PASS: media_assets con content_item_id del mismo space.
-- EXPECT PASS: content_events con content_item_id del mismo space y payload
--              minimo/sanitizado.
-- EXPECT PASS: hidden representa marker/restauracion futura, no hard delete
--              por defecto.

-- ============================================================================
-- 9. Casos invalidos esperados - SOLO COMENTARIOS EXPECT FAIL
-- ============================================================================
--
-- EXPECT FAIL: local sin local_id.
-- EXPECT FAIL: local con base_id.
-- EXPECT FAIL: override sin base_id.
-- EXPECT FAIL: override con local_id.
-- EXPECT FAIL: hidden sin base_id.
-- EXPECT FAIL: hidden con local_id.
-- EXPECT FAIL: override cross-space.
-- EXPECT FAIL: hidden cross-space.
-- EXPECT FAIL: media cross-space.
-- EXPECT FAIL: event cross-space.
-- EXPECT FAIL: external_user insert.
-- EXPECT FAIL: hard delete content_items.
-- EXPECT FAIL: update sensitive columns.
-- EXPECT FAIL: direct membership write.
-- EXPECT FAIL: quitar ultimo owner.

-- ============================================================================
-- 10. Reset / rollback
-- ============================================================================
--
-- Este archivo NO incluye reset.
--
-- Rollback principal:
-- - destruir el proyecto Supabase desechable.
--
-- Reset SQL:
-- - sera una fase separada;
-- - no debe mezclarse con este plan;
-- - nunca debe usarse contra produccion;
-- - debe confirmar el project ref dos veces antes de cualquier SQL futuro.

-- Expected results reference:
-- - La matriz esperada vive en docs/supabase/fixtures/README.md.
-- - Revisar la matriz T01-T20 antes de cualquier ejecucion futura aislada.
-- - Este draft comentado no define por si solo el resultado final.
-- - Cualquier ejecucion futura debe validarse contra esa matriz.
-- - Si la matriz y este SQL draft divergen, detener la ejecucion y actualizar
--   la documentacion antes de aplicar cualquier SQL.
-- - Esta referencia no aplica SQL ni valida Supabase.

-- ============================================================================
-- 11. Checklist pre-ejecucion futura
-- ============================================================================
--
-- [ ] Entorno desechable confirmado.
-- [ ] Project ref confirmado dos veces.
-- [ ] Schema/RLS aplicados manualmente en entorno aislado.
-- [ ] Identidades Auth sinteticas creadas o mapeadas.
-- [ ] UUIDs placeholders reemplazados por UUIDs del entorno desechable.
-- [ ] No datos reales.
-- [ ] No emails reales.
-- [ ] No service-role.
-- [ ] No produccion.
-- [ ] Reset/rollback definido.
-- [ ] Aprobacion explicita antes de ejecutar cualquier SQL.
--
-- FIN DEL DRAFT DOCUMENTAL. NO EJECUTAR.
